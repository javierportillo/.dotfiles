.shell powershell

# Reload whkd configuration
# alt + o                 : taskkill /f /im whkd.exe && start /b whkd # if shell is cmd
alt + o                 : taskkill /f /im whkd.exe; if ($?) { Start-Process whkd -WindowStyle hidden } # if shell is pwsh / powershell
alt + shift + o         : komorebic reload-configuration

# Focus windows
alt + h                 : komorebic focus left
alt + j                 : komorebic focus down
alt + k                 : komorebic focus up
alt + l                 : komorebic focus right
alt + shift + oem_4     : komorebic cycle-focus previous # oem_4 is [
alt + shift + oem_6     : komorebic cycle-focus next # oem_6 is ]

# Move windows
alt + shift + h         : komorebic move left
alt + shift + j         : komorebic move down
alt + shift + k         : komorebic move up
alt + shift + l         : komorebic move right
alt + shift + return    : komorebic promote

# Stack windows
alt + left              : komorebic stack left
alt + down              : komorebic stack down
alt + up                : komorebic stack up
alt + right             : komorebic stack right
alt + oem_1             : komorebic unstack # oem_1 is f
alt + oem_4             : komorebic cycle-stack previous # oem_4 is [
alt + oem_6             : komorebic cycle-stack next # oem_6 is ]

# Resize
alt + oem_plus          : komorebic resize-axis horizontal increase
alt + oem_minus         : komorebic resize-axis horizontal decrease
alt + shift + oem_plus  : komorebic resize-axis vertical increase
alt + shift + oem_minus : komorebic resize-axis vertical decrease

# Manipulate windows
alt + t                 : komorebic toggle-float
alt + shift + f         : komorebic toggle-monocle
alt + q                 : komorebic close

# Window manager options
alt + shift + r         : komorebic retile
alt + p                 : komorebic toggle-pause

# Layouts
alt + x                 : komorebic flip-layout horizontal
alt + y                 : komorebic flip-layout vertical
alt + shift + oem_period : komorebic cycle-layout next

# Workspaces
alt + 1                 : komorebic focus-workspace 0
alt + 2                 : komorebic focus-workspace 1
alt + 3                 : komorebic focus-workspace 2

# Move windows across workspaces
# I like to send more than move to workspace
# and then switch myself
alt + shift + 1         : komorebic send-to-workspace 0
alt + shift + 2         : komorebic send-to-workspace 1
alt + shift + 3         : komorebic send-to-workspace 2

# Move windows across monitors
alt + shift + 8         : komorebic move-to-monitor 0
alt + shift + 9         : komorebic move-to-monitor 1
alt + shift + 0         : komorebic move-to-monitor 2

# App shortcuts - these require shell to be pwsh / powershell
# The apps will be focused if open, or launched if not open
ctrl + alt + shift + c  : if ($wshell.AppActivate('Chrome') -eq $False) { start chrome }
ctrl + alt + shift + f  : if ($wshell.AppActivate('Files') -eq $False) { start Files }
ctrl + alt + shift + t  : if ($wshell.AppActivate((Write-Output (Get-Process -Name "WindowsTerminal").Id)) -eq $False) { start wt }
ctrl + alt + shift + z  : if ($wshell.AppActivate((Write-Output (Get-Process -Name "wezterm-gui").Id)) -eq $False) { start wezterm-gui.exe }
ctrl + alt + shift + d  : if ($wshell.AppActivate('Discord') -eq $False) { Start-Process (Join-Path $env:LOCALAPPDATA "Discord\Update.exe") -ArgumentList "--processStart Discord.exe" }

